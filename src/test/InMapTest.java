import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

public class InMapTest {

    /**

    TEST FOR PREVIOUS LAB -> OUTDATED NOW
    @Test
    public void leaveUp(){
        Animal testAnimal = new Animal();
        for (int i = 0; i<=3; i++){
            testAnimal.move(MoveDirection.FORWARD);
        }
        assertEquals("position: (2,4) orientation: Północ" ,testAnimal.toString());
    }

    @Test
    public void leaveRight(){
        Animal testAnimal = new Animal();
        testAnimal.move(MoveDirection.RIGHT);
        for (int i = 0; i<=3; i++){
            testAnimal.move(MoveDirection.FORWARD);
        }
        assertEquals("position: (4,2) orientation: Wschód" ,testAnimal.toString());
    }

    @Test
    public void leaveDown(){
        Animal testAnimal = new Animal();
        testAnimal.move(MoveDirection.RIGHT);
        testAnimal.move(MoveDirection.RIGHT);
        for (int i = 0; i<=3; i++){
            testAnimal.move(MoveDirection.FORWARD);
        }
        assertEquals("position: (2,0) orientation: Południe" ,testAnimal.toString());
    }

    @Test
    public void leaveLeft(){
        Animal testAnimal = new Animal();
        testAnimal.move(MoveDirection.LEFT);
        for (int i = 0; i<=3; i++){
            testAnimal.move(MoveDirection.FORWARD);
        }
        assertEquals("position: (0,2) orientation: Zachód" ,testAnimal.toString());
    }
    */
}
